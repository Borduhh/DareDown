# Typography

## Heading level two

### Heading level three

#### Heading level four

##### Heading level five

Body copy sets the rhythm for everything else. The measure is capped so lines
stay readable, and the leading is loose enough to skim. Smart quotes turn "this"
into curly quotes, and -- becomes an en dash.

A second paragraph, to check the space between blocks. Then a list:

- First item
- Second item, long enough to wrap onto a second line so the hanging indent is
  visible against the bullet
- Third item

| Column | Value |
|---|---|
| Alpha | 1 |
| Beta | 2 |

Inline `code` inside a sentence, and a fenced block:

```ts
type Theme = 'system' | 'light' | 'dark';

const resolve = (t: Theme, systemDark: boolean): boolean =>
  t === 'system' ? systemDark : t === 'dark';
```

> Closing quotation, to check the italic treatment and the left rule.
